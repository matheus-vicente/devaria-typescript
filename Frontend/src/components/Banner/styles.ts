import styled from "styled-components";

export const Container = styled.section`
  img {
    margin-bottom: 2rem;
    cursor: pointer;
  }
  h1 {
    font-size: 2.4rem;
    color: var(--text-blue);
  }
`;
